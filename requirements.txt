qdrant-client[fastembed]>=1.12
sentence-transformers>=3.0
fastapi>=0.115
uvicorn>=0.30
pydantic>=2.0
httpx>=0.27
anthropic>=0.40
prefect>=3.0
mwparserfromhell>=0.6
pyjwt>=2.8

# Optional: expose the corpus to MCP clients (Claude Desktop) via src/mcp_server.py
mcp>=2.0

# Dev / CI
pytest>=8.0
ruff>=0.6
